#include <stdio.h>
int main(void){
    int cprogressivo;
    printf("O valor da variaveis'cprogressivo' eh %d\n",cprogressivo);
    //Modificadores do tipo Inteiro
    //Descobrir o valor do int(tamanho)
    printf("O tamanho do inteiro em meu computador: %d bytes\n",sizeof(int));
    printf("O tamanho do short int: %d bytes\n", sizeof(short));
    printf("O tamanho do long int: %d bytes\n", sizeof(long));
}

